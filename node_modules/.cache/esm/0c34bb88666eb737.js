let jwt;_666‍.x([["default",()=>_666‍.o]]);_666‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const authGuard = async (req, res, next) => {
    if (req.headers.authorization) {
        try {
            req.user =jwt.verify(req.headers.authorization.split('Bearer ')[1], 'authorization')
            next()
        }catch (e) {
            res.send({error : e})
        }
    }else {
        res.send('No Token')
    }
}

const checkUser = async (req, res, next) => {
    if (req.user) {
        next()
    }else {
        res.send('No user')
    }
}

_666‍.d({authGuard, checkUser});
